[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools"
]

[project]
name = "ner"
version = "0.1.0"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = {"" = "src"}
